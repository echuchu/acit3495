apiVersion: apps/v1   # how the program will read this file
kind: Deployment  # type of deployment (pod, RS, deployment)
metadata:
  name: auth-deploy  # name of the deployment
  labels:
    app: auth  # label of the deployment
    service: loadbalancer
spec:
  replicas: 1   # number of pods
  selector:
    matchLabels:
      app: auth  # what labels the pods should match with
  template:
    metadata:
      labels:
        app: auth  # label each pod should have
    spec:
      terminationGracePeriodSeconds: 60
      containers:
      - name: "auth-server"   # name of the pod
        image: echuchu/auth   # image the pod should use
        imagePullPolicy: Always   # specifies whether it should use the local copy or pull from dockerhub
        ports:
        - containerPort: 8080     # port of the container
        env:
          - name: BASIC_AUTH_USERNAME
            valueFrom:
              secretKeyRef:
                name: secrets
                key: basic-auth-username
          - name: BASIC_AUTH_PASSWORD
            valueFrom:
              secretKeyRef:
                name: secrets
                key: basic-auth-password
          # - name: SERVER_NAME
          #   valueFrom:
          #     configMapKeyRef:
          #       name: configs
          #       key: server-name
          - name: PORT
            valueFrom:
              configMapKeyRef:
                name: configs
                key: port

---

# apiVersion: v1
# kind: Service
# metadata:
#   name: auth-service
# spec:
#   selector:
#     app: auth
#   ports:
#     - protocol: TCP
#       port: 80
#       targetPort: 8080